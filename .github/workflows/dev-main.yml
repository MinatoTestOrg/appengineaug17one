name: dev Runner

on: workflow_dispatch

jobs:
  deployment:
    environment: dev
    runs-on: ubuntu-latest
    container: node:16-alpine

    steps:
    - name: Content...
      run: |
        echo "${{ vars.username }}"
        echo "${{ vars.password }}"
